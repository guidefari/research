{
  "name": "research",
  "private": true,
  "workspaces": ["web"],
  "scripts": {
    "dev":         "bun run --cwd web dev",
    "build":       "bun run --cwd web build",
    "preview":     "bun run --cwd web preview",
    "screenshots": "bun run --cwd web screenshots"
  }
}
